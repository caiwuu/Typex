const testPlugin = require('./jsxPlugin.js')
module.exports = {
  presets: [
    [
      '@babel/preset-env',
      {
        targets: {
          node: 'current',
        },
      },
    ],
  ],
  plugins: [testPlugin],
}
